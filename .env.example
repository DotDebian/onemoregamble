# Copy to .env and fill in. The chat panel needs an Anthropic API key.
# Either of these works (NUXT_ prefix maps to runtimeConfig; the SDK also
# reads the unprefixed ANTHROPIC_API_KEY directly):
ANTHROPIC_API_KEY=sk-ant-...
# NUXT_ANTHROPIC_API_KEY=sk-ant-...
