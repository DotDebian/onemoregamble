# pnpm settings (single app — no workspace packages).
# Allow these trusted dependencies to run their build scripts during install,
# otherwise pnpm 11 aborts with ERR_PNPM_IGNORED_BUILDS in CI/Docker.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
